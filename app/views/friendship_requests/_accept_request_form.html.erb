<%= form_for(current_user.friendships.build) do |f| %>
    <%= hidden_field_tag :friend_id, friendship_request.user.id %>
    <%= f.submit "Accept Friend Request", class: "accept friend-button" %>
<% end %>