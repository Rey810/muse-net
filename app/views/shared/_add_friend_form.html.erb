<div id='add_<%= id_user(user) %>'>
    <%= form_for(current_user.friendship_requests.build, remote: true) do |f| %>
        <%= hidden_field_tag :to_user_id, user.id %>
        <%= f.submit "Add friend", class: "add friend-button" %>
    <% end %>
</div>

